<div align="center">

# Реализация структуры skip-list на C++
  ![C++](https://img.shields.io/badge/C++-23-blue?style=for-the-badge&logo=cplusplus)
  ![CMake](https://img.shields.io/badge/CMake-3.20+-green?style=for-the-badge&logo=cmake)
  
</div>

## Запуск программы 🐼
- Клонирование, сборка и компиляция программы выполняется следующими командами:

```
git clone [https://github.com/RTCupid/Skip_list.git](https://github.com/RTCupid/Skip_list.git)
cd Skip_list
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

- Запуск тестов:
```
./build/ctest
```

## Документация 🧩

Ниже можно ознакомиться с документацией к программе, сгенерированной при помощи `Doxygen`:

- [Документация](https://rtcupid.github.io/Skip_list/)

 ## Введение ⭐

`Скип-лист` — вероятностная структура данных, предназначенная для хранения упорядоченного множества уникальных элементов.
Обеспечивает операции вставки, удаления и поиска за `O(log n)` в среднем при сравнительно простой реализации.

## Структура 📐
 
Скип-лист состоит из нескольких уровней связанных списков:

- Уровень `0` содержит все элементы в отсортированном порядке.

- Каждый следующий уровень является «экспресс‑линией»: он содержит часть элементов предыдущего уровня.

- Вероятность того, что элемент попадёт на уровень `i`, равна `pⁱ⁻¹` (обычно `p = 0.5`).

- Головной узел (`head`) — фиктивный элемент, который присутствует на всех уровнях и хранит значение ключа по умолчанию.

**Важно**: `количество уровней не фиксировано`. Оно растёт при вставке узлов с высоким уровнем и может уменьшаться при удалении элементов с верхних уровней.

## Генерация уровня нового узла 🎲
При вставке для нового узла генерируется случайный уровень:

1. Начальный уровень = `1`.

2. С вероятностью p уровень увеличивается на `1`.

3. Процесс повторяется, пока:

- генератор не выдаст значение `≥ p`,

- либо не будет достигнут максимально допустимый уровень (`maxAllowedLevel`),

- либо не будет превышен текущий максимум уровней `+ 1`.

Такой подход гарантирует, что примерно половина узлов будет на уровне `1`, четверть — на уровне `2` и т.д.

## Поиск элемента 🔍
Поиск начинается с самого верхнего уровня головы.
На каждом уровне движемся вперёд, пока ключ следующего узла меньше искомого.
Когда следующего узла нет или его ключ больше, спускаемся на уровень ниже.
На уровне 0 проверяем следующий узел — если его ключ равен искомому, элемент найден.

## Вставка элемента ➕
1. Выполняем поиск, запоминая предшественников на каждом уровне.

2. Если ключ уже существует — вставка не выполняется (множество уникальных ключей).

3. Генерируем случайный уровень `newLevel` для нового узла.

4. Если `newLevel` превышает текущую высоту списка (`maxLevel_`):

- расширяем голову и вектор предшественников,

- увеличиваем `maxLevel_`.

5. Создаём новый узел с уровнем `newLevel`.

6. На каждом уровне от `0` до `newLevel-1` включаем узел в список, корректируя указатели предшественников.

## Удаление элемента ❌
1. Выполняем поиск, запоминая предшественников.

2. Если элемент не найден — возвращаем `false`.

3. На каждом уровне, где присутствует удаляемый узел, перенаправляем указатель предшественника на следующий узел.

4. Удаляем узел.

5. Если верхние уровни опустели, уменьшаем `maxLevel_` и урезаем вектор указателей головы.

## Печать по уровням 🖨️
Метод `printByLevels()` выводит содержимое каждого уровня от самого высокого до нулевого.
Каждый уровень отображается как строка ключей, разделённых пробелами.
Это полезно для отладки и визуализации распределения элементов.

## Итератор 🔁
`Скип-лист` предоставляет однонаправленный итератор (`Forward Iterator`):

- Проходит по элементам в порядке возрастания (уровень 0).

- Обеспечивает только чтение (ключи неизменяемы).

- Поддерживает range‑based for и стандартные алгоритмы STL.

## Требования к типам ⚙️
Тип ключа `Key` должен быть сравним через оператор `<` (LessThanComparable).

Должен иметь конструктор по умолчанию (используется для головного узла).

## Производительность 📊
| Операция | Средняя сложность |	Худшая сложность |
|----------|-------------------|-------------------|
| Поиск	| O(log n) |	O(n) |
| Вставка |	O(log n) |	O(n) |
| Удаление |	O(log n)	| O(n) |
| Память |	O(n) |	O(n) |

Худший случай возникает при неудачной генерации уровней (например, все узлы имеют уровень `1`), но вероятность этого экспоненциально мала при увеличении `n`.

## Пример использования 📁
```C++
SkipList<int> list(0.5);
list.insert(10);
list.insert(5);
list.insert(15);

for (int x : list) {
    std::cout << x << " "; // 5 10 15
}

list.erase(10);
std::cout << list.contains(10); // false
list.printByLevels();
```
